CREATE TABLE T_HOLIDAY_DETAIL (D_ID VARCHAR(36) NOT NULL, CREATED_BY VARCHAR, CREATED_ON TIMESTAMP, DATE VARCHAR, DAY_WEEK VARCHAR, MONTH VARCHAR, PH_DESCRIPTION VARCHAR, PH_ID VARCHAR, UPDATED_BY VARCHAR, UPDATED_ON TIMESTAMP, YEAR VARCHAR, PRIMARY KEY (D_ID))
